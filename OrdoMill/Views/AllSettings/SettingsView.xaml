<UserControl x:Class="OrdoMill.Views.AllSettings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:extensions="clr-namespace:SmartApp.Helpers.Extensions;assembly=SmartApp.Helpers"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="600"
             d:DesignWidth="1300"
             DataContext="{Binding Source={StaticResource Locator},
                                   Path=SettingsViewModel}"
             Style="{DynamicResource CustomUserControl}"
             mc:Ignorable="d">
    <TabControl>
        <TabItem Header="Database Settings">
            <Grid Margin="20" extensions:MarginSetter.Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.Resources>
                    <ResourceDictionary Source="../../Resources/Styles/ButtonsStyle.xaml" />
                </Grid.Resources>
                <Button Grid.Column="1" Command="{Binding SetDbPathCommand}">
                    <StackPanel>
                        <TextBlock Text="Change Database" />
                        <ContentControl Content="{DynamicResource DataSettings}" />
                    </StackPanel>
                </Button>
                <Label Grid.Row="1" Content="Item par page:" />
                <controls:NumericUpDown Grid.Row="1"
                                        Grid.Column="1"
                                        Value="{Binding PageSize}" />
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>
