<UserControl x:Class="OrdoMill.Views.Medecins.MedecinsDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:extensions="clr-namespace:SmartApp.Helpers.Extensions;assembly=SmartApp.Helpers"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{Binding Source={StaticResource Locator},
                                     Path=MedecinsViewModel}"
             d:DesignWidth="300"
             Style="{DynamicResource EditBoxStyle}"
             mc:Ignorable="d">
    <Grid VerticalAlignment="Top"
          extensions:MarginSetter.Margin="3"
          IsEnabled="{Binding IsEditable}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="1"
                 VerticalAlignment="Center"
                 CharacterCasing="Upper"
                 Text="{Binding SelectedItem.Nom,
                                Mode=TwoWay}" />
        <Label Grid.Row="0"
               HorizontalAlignment="Left"
               VerticalAlignment="Center"
               Content="{DynamicResource NameD}" />
        <Label Grid.Row="1"
               HorizontalAlignment="Left"
               VerticalAlignment="Center"
               Content="{DynamicResource SpecialiteD}" />
        <ComboBox Grid.Row="1"
                  Grid.Column="1"
                  VerticalAlignment="Center"
                  ItemsSource="{DynamicResource MedecinsTypes}"
                  SelectedIndex="{Binding SelectedItem.Type}" />
        <Label Grid.Row="2"
               HorizontalAlignment="Left"
               VerticalAlignment="Center"
               Content="{DynamicResource TeleD}" />
        <TextBox Grid.Row="2"
                 Grid.Column="1"
                 VerticalAlignment="Center"
                 Text="{Binding SelectedItem.Tel,
                                Mode=TwoWay}" />
        <Label Grid.Row="3"
               HorizontalAlignment="Left"
               VerticalAlignment="Center"
               Content="{DynamicResource AddressD}" />
        <TextBox Grid.Row="3"
                 Grid.Column="1"
                 VerticalAlignment="Center"
                 Text="{Binding SelectedItem.Adresse}" />
    </Grid>
</UserControl>