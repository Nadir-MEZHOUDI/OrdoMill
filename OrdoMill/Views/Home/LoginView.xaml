<UserControl x:Class="OrdoMill.Views.Home.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:extensions="clr-namespace:SmartApp.Helpers.Extensions;assembly=SmartApp.Helpers"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             MaxWidth="700"
             d:DataContext="{Binding Main.LoginDialog.ViewModel,
                                     Source={StaticResource Locator}}"
             Background="Transparent"
             Style="{DynamicResource CustomUserControl}"
             mc:Ignorable="d">

    <Grid Margin="5" extensions:MarginSetter.Margin="3">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="150" />
            <ColumnDefinition />
            <ColumnDefinition Width="150" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Grid.ColumnSpan="2"
               HorizontalAlignment="Center"
               Content="{DynamicResource Login}"
               FontSize="30" />
        <Label Grid.Row="1"
               Grid.Column="1"
               Grid.ColumnSpan="2"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Content="{DynamicResource EntreDonnerMsg}" />
        <Label Grid.Row="2" Content="{DynamicResource UserName}" />
        <ComboBox Grid.Row="2"
                  Grid.Column="1"
                  Grid.ColumnSpan="3"
                  VerticalAlignment="Center"
                  controls:TextBoxHelper.Watermark="{DynamicResource EntreUserNameHint}"
                  FontSize="15"
                  IsEditable="True"
                  ItemsSource="{Binding Users,
                                        UpdateSourceTrigger=PropertyChanged}"
                  Text="{Binding UserName,
                                 Mode=TwoWay,
                                 UpdateSourceTrigger=PropertyChanged}" />
        <Label Grid.Row="3" Content="{DynamicResource Password}" />
        <PasswordBox Grid.Row="3"
                     Grid.Column="1"
                     Grid.ColumnSpan="3"
                     VerticalAlignment="Center"
                     controls:PasswordBoxHelper.CapsLockWarningToolTip="لوحة المفاتيح مغلقة"
                     controls:TextBoxHelper.Watermark="{DynamicResource EntrePasswordHint}"
                     extensions:PasswordHelper.Attach="True"
                     extensions:PasswordHelper.Password="{Binding Password,
                                                                  Mode=TwoWay,
                                                                  UpdateSourceTrigger=PropertyChanged,
                                                                  ValidatesOnDataErrors=True,
                                                                  NotifyOnValidationError=True}"
                     FontSize="15" />
        <Button Grid.Row="4"
                Grid.Column="1"
                Height="30"
                
                Command="{Binding HideDialogIfPossibleCommand}"
                Content="{DynamicResource Enter}"
                IsDefault="True"
                Padding="2"
                Style="{DynamicResource AccentedSquareButtonStyle}" />
        <Button Grid.Row="4"
                Grid.Column="3"
                Height="30"
                
                Command="{Binding CloseCommand}"
                Content="{DynamicResource Close}"
                Padding="0"
                Style="{DynamicResource AccentedSquareButtonStyle}" />
        <TextBlock Grid.Row="5"
                   Grid.ColumnSpan="4"
                   Margin="5"
                   HorizontalAlignment="Stretch"
                   Foreground="{DynamicResource GrayBrush1}"
                   Text="{Binding ErrorMessage,
                                  UpdateSourceTrigger=PropertyChanged}"
                   TextAlignment="Center" />
    </Grid>
</UserControl>