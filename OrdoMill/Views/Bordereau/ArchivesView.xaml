<UserControl x:Class="OrdoMill.Views.Bordereau.ArchivesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:bordereau="clr-namespace:OrdoMill.Views.Bordereau"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             xmlns:extensions="clr-namespace:OrdoMill.Helpers.Extensions"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="500"
             d:DesignWidth="1200"
             dialog:DialogParticipation.Register="{Binding}"
             Style="{DynamicResource CustomUserControl}"
             mc:Ignorable="d">
    <Grid extensions:MarginSetter.Margin="3">
        <TabControl>
            <TabItem>
                <TabItem.Header>
                    <TextBlock Width="200"
                               Margin="0,-5"
                               FontWeight="Bold"
                               Text="{DynamicResource Bordereaux}"
                               TextAlignment="Center" />
                </TabItem.Header>
                <bordereau:BordereauView />
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <TextBlock Width="200"
                               Margin="0,-5"
                               FontWeight="Bold"
                               Text="{DynamicResource Factures}"
                               TextAlignment="Center" />
                </TabItem.Header>
                <bordereau:FacturesView />
            </TabItem>
            <!--<controls:MetroTabItem FontStyle="Italic" Header="{DynamicResource Cascade}">
                <bordereau:CascadeView DataContext="{Binding ArchiveViewModel, Source={StaticResource Locator}}" />
            </controls:MetroTabItem>-->
        </TabControl>
    </Grid>
</UserControl>
