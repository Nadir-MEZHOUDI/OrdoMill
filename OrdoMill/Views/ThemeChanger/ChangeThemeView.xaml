<UserControl x:Class="OrdoMill.Views.ThemeChanger.ChangeThemeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:themeChanger="clr-namespace:OrdoMill.Views.ThemeChanger"
             d:DesignHeight="600"
             Background="{DynamicResource AccentColorBrush}"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <themeChanger:ChangeThemeVm />
    </UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/FlatSlider.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Ellipse x:Key="AppThemeMenuIcon"
                     Width="16"
                     Height="16"
                     x:Shared="False"
                     Fill="{Binding ColorBrush,
                                    Mode=OneWay}"
                     Stroke="{Binding BorderColorBrush,
                                      Mode=OneWay}" />
            <Ellipse x:Key="AccentMenuIcon"
                     Width="16"
                     Height="16"
                     x:Shared="False"
                     Fill="{Binding ColorBrush,
                                    Mode=OneWay}" />
            <Style x:Key="AccentColorMenuItemStyle" TargetType="{x:Type MenuItem}">
                <Setter Property="Command" Value="{Binding ChangeAccentCommand}" />
                <Setter Property="Icon" Value="{StaticResource AccentMenuIcon}" />
                <Setter Property="Header" Value="{Binding Name, Mode=OneWay}" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>


        <!--<ListBox Margin="10" ItemsSource="{Binding AppThemes}"  BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Button Background="{Binding ColorBrush}"
                            Command="{Binding ChangeAccentCommand}"
                            FontSize="15"
                            Foreground="{Binding BorderColorBrush}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Ellipse Width="16"
                                     Height="16"
                                     Margin="25,0"
                                     Fill="{Binding BorderColorBrush,
                                                    Mode=OneWay}"
                                     Stroke="{Binding ColorBrush,
                                                      Mode=OneWay}" />
                            <TextBlock Grid.Column="1" Text="{Binding Name}" />
                        </Grid>
                    </Button>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>-->
        <ListBox Grid.Row="1"
                 Width="200"
                 HorizontalContentAlignment="Stretch"
                 Background="{DynamicResource AccentColorBrush}"
                 BorderThickness="0"
                 ItemsSource="{Binding AccentColors}"
                 Padding="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Button Grid.ColumnSpan="2"
                                Height="25"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                controls:ButtonHelper.PreserveTextCase="True"
                                BorderThickness="0"
                                Command="{Binding ChangeAccentCommand}"
                                Content="{Binding Name}"
                                FontSize="15" />
                        <Ellipse Width="18"
                                 Height="18"
                                 Margin="25,5"
                                 Fill="{Binding ColorBrush}" />

                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
